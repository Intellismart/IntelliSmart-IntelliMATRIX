# Copy to .env and fill in your connection string
MONGODB_URI=
# Optional: override database name (defaults to intellitrader)
MONGODB_DB=intellitrader

# Session secret for signing cookies (set a strong random string in production)
SESSION_SECRET=change-me
